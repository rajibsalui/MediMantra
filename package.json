{
  "name": "medimantra",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf client/.next client/node_modules server/node_modules",
    "build":"npm install --prefix server && npm install --prefix client && npm install --prefix api && npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
